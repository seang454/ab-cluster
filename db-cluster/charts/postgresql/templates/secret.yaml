# ExternalSecret — External Secrets Operator syncs this from Vault into a
# standard Kubernetes Secret. The CNPG operator reads it like any normal Secret.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "postgresql.superuserSecretName" . }}
  namespace: {{ .Release.Namespace }}
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "postgresql.superuserSecretName" . }}
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      data:
        username: "postgres"
        password: "{{ `{{ .superuserPassword }}` }}"
  data:
  - secretKey: superuserPassword
    remoteRef:
      key: databases/postgresql
      property: superuser-password
  - secretKey: appPassword
    remoteRef:
      key: databases/postgresql
      property: app-password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "postgresql.appSecretName" . }}
  namespace: {{ .Release.Namespace }}
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: {{ include "postgresql.appSecretName" . }}
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
      data:
        username: {{ .Values.cluster.bootstrap.owner | quote }}
        password: "{{ `{{ .appPassword }}` }}"
  data:
  - secretKey: appPassword
    remoteRef:
      key: databases/postgresql
      property: app-password
