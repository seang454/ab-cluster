apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cassandra.secretName" . }}
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  username: {{ .Values.credentials.username | quote }}
  password: {{ .Values.credentials.password | quote }}
