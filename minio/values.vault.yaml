auth:
  existingSecret: minio-credentials
  rootUserKey: root-user
  rootPasswordKey: root-password

bucketProvisioning:
  enabled: true
  # setup.sh overrides this to the active database namespace.
  bucket: databases
