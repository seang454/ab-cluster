{
  "spec": {
    "target": {
      "template": {
        "data": {
          "AWS_ACCESS_KEY_ID": "{{ index . \"root-user\" }}",
          "AWS_SECRET_ACCESS_KEY": "{{ index . \"root-password\" }}",
          "ACCESS_KEY_ID": "{{ index . \"root-user\" }}",
          "SECRET_ACCESS_KEY": "{{ index . \"root-password\" }}",
          "access-key-id": "{{ index . \"root-user\" }}",
          "secret-access-key": "{{ index . \"root-password\" }}",
          "root-user": "{{ index . \"root-user\" }}",
          "root-password": "{{ index . \"root-password\" }}",
          "credentials": "[default]\naws_access_key_id = {{ index . \"root-user\" }}\naws_secret_access_key = {{ index . \"root-password\" }}\n"
        }
      }
    }
  }
}